#spring.application.name=application
#spring.profiles.active=dev
#spring.cloud.config.uri=http://localhost:8888,http://localhost:9999
#spring.cloud.config.fail-fast=true

spring:
  application:
    name: application
  profiles:
    active: dev
  cloud:
    config:
     uri: http://localhost:8888,http://localhost:9999
     fail-fast: true